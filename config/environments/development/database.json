{
  "defaultConnection": "default",
  "connections": {
    "default": {
      "connector": "mongoose",
      "settings": {
        "database": "strapi",
        "host": "localhost",
        "srv": false,
        "port": "27017",
        "username": "admin",
        "password": "admin"
      },
      "options": {
        "authenticationDatabase": "strapi"
      }
    }
  }
}
